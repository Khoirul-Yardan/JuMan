
import 'package:flutter/material.dart';

class ViewFileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('View File')),
      body: Center(child: Text('Open file flow - decrypt to temp and open external editor')),
    );
  }
}
